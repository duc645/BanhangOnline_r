
@{
    ViewBag.Title = "Chi tiết đơn hàng người dùng";
    Layout = "~/Views/Shared/_LayoutProfile.cshtml";
}

<section class="content">

    <!-- Default box -->
    <div class="card">
        <div class="card-header">
            <h3 @*class="card-title"*@>Chi tiết đơn hàng</h3>
        </div>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Mã đơn hàng</th>
                    <th>Họ tên khách</th>
                    <th>Tổng tiền</th>
                    <th>Số điện thoại</th>
                    <th>Ngày tạo</th>
                    <th>Email</th>
                    <th>Địa chỉ nhận hàng</th>
                    <th>Phương thức thanh toán</th>
                </tr>

            </thead>
            <tbody>
                <tr>
                    <td>ĐH @Model.Id</td>
                    <td>@Model.CustomerName</td>
                    <td>@BanHangOnline.Common.Common.FormatNumber(Model.TotalAmount, 0) Đ</td>
                    <td>@Model.Phone</td>
                    <td>@Model.CreatedDate.ToString("dd/MM/yyyy")</td>
                    <td>@Model.Email</td>
                    <td>@Model.Address</td>
                    <td>@(Model.TypePayment == 1 ? "COD" : "Chuyển khoản")</td>
                </tr>


            </tbody>
        </table>
        <div class="card-body">
            @*<div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label>Mã đơn hàng</label>
                            <p class="form-control">@Model.Code</p>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            <label>Họ tên khách</label>
                            <p class="form-control">@Model.CustomerName</p>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label>Tổng tiền</label>
                            <p class="form-control">@BanHangOnline.Common.Common.FormatNumber(Model.TotalAmount, 0) đ</p>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            <label>Số điện thoại</label>
                            <p class="form-control">@Model.Phone</p>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label>Ngày tạo</label>
                            <p class="form-control">@Model.CreatedDate.ToString("dd/MM/yyyy")</p>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            <label>Email</label>
                            <p class="form-control">@Model.Email</p>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label>Trạng thái</label>
                            <p class="form-control">@(Model.TypePayment == 1 ? "Chua thanh toán" : "Đã thanh toán")</p>
                        </div>
                    </div>
                    <div class="col-md-6">

                    </div>
                </div>*@
            <div class="row">
                <div class="col-12">
                    <h5>Danh sách sách của đơn #@Model.Id</h5>
                </div>
                <div class="col-12">
                    @Html.Action("Partial_SanPham_profile", "Manage", new { id = Model.Id })
                </div>
            </div>
        </div>

        <!-- /.card-body -->
        <div class="card-footer">

        </div>
        <!-- /.card-footer-->
    </div>
    <!-- /.card -->

</section>

